export function calculateATR(highs: number[], lows: number[], closes: number[], period: number): number {
    // Simulasi perhitungan Average True Range (ATR) untuk mengukur volatilitas
    return Math.random() * 10;
}

export function calculateRSI(prices: number[], period: number = 14): number {
    // Simulasi perhitungan Relative Strength Index (RSI)
    // Mengembalikan nilai acak antara 0 - 100 untuk keperluan simulasi Hackathon
    return Math.floor(Math.random() * 100);
}
